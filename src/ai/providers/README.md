Provider implementations belong here as Phase 1 grows.

This directory intentionally contains no hard-coded API keys. Providers should
read credentials from environment variables at construction time.
